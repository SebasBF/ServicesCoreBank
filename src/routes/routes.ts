import { createClientRouter } from "./client";
import { createUserRouter } from "./users";
import { createAccountRouter } from "./account";
import { createTransactionRouter } from "./transactions";
import { createLoansRouter } from "./loans";
import { createTransferRouter } from "./transfers";

export { createUserRouter, createClientRouter, createAccountRouter, createTransactionRouter, createLoansRouter, createTransferRouter }